name: flutter_bankruptcy
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.0-138.0.dev

# Add regular dependencies here.
dependencies:
  http: ^0.13.5
  intl: ^0.18.0
  uri: ^1.0.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
